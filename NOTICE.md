# Notice

Kinetic Toast includes software derived from the original `sileo` package by Aaryan:

- npm: https://www.npmjs.com/package/sileo
- GitHub: https://github.com/hiaaryan/sileo

The original npm package metadata declares the project license as MIT. Kinetic Toast preserves MIT licensing and uses a distinct package name (`kinetic-toast`), public API (`kinetic`), and CSS namespace (`data-kinetic-*`, `--kinetic-*`).
